<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp">

    <!-- Empty State Icon -->
    <ImageView
        android:id="@+id/ivEmptyIcon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginBottom="24dp"
        android:src="@drawable/empty_state_universal"
        android:contentDescription="Empty history state" />

    <!-- Title -->
    <TextView
        android:id="@+id/tvEmptyTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="Belum Ada Riwayat"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/text_secondary"
        android:gravity="center" />

    <!-- Description -->
    <TextView
        android:id="@+id/tvEmptyDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:text="Riwayat pembelajaran Anda akan tampil di sini setelah Anda mulai berlatihan."
        android:textSize="14sp"
        android:textColor="@color/text_hint"
        android:gravity="center"
        android:lineSpacingExtra="2dp"
        android:maxWidth="280dp" />

    <!-- Action Button -->
    <Button
        android:id="@+id/btnAction"
        style="@style/Widget.PembelajaranKaidah.Button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mulai Latihan"
        android:textSize="14sp" />

</LinearLayout>